# Claude repository bootstrap

Use `AGENTS.md` as the primary repository operating contract. Rehydrate `docs/GLOBAL_POLICY_KERNEL.md` and the task-relevant entries in `control-plane/ai-system/configs/global-policy-manifest.json` before substantive work when instruction state is unknown or stale.

Before material action, apply `docs/GOAL_FIDELITY_AND_TARGET_LOCK_POLICY.md` and `control-plane/ai-system/configs/goal-fidelity-global.json`. Build the Goal Contract, run five independent target-identification checks, escalate to the additional five checks when material ambiguity remains, preserve `GOAL_SIGNATURE`, and recheck for drift after route/environment/context changes and before release.

When available for substantive tasks, treat GitHub as executable/source/version/test truth and Notion as durable cross-repository decision/research/context memory. Never infer successful connector use from configuration or file presence alone.

Prefer causally distinct branches over repeated variants of the same hypothesis. Preserve required capability while changing routes, and require observable evidence before PASS.
